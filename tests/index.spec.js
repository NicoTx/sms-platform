'use strict'

/* global describe, it */
const chai = require('chai')
const expect = chai.expect
const request = require('supertest')
const app = require('../index')

describe('api', function () {
  it('doit exposer une route /', function (done) {
    // @TODO tester les routes de l'API
  })
})
